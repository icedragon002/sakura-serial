name: Build & Release

on:
  push:
    tags:
      - 'v*'           # v1.0.0, v1.1.0 etc
  workflow_dispatch:    # allow manual trigger from Actions tab

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build & Package
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run package

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}
          path: |
            dist/*.exe
            dist/*.dmg
            dist/*.AppImage
            dist/*.deb
          if-no-files-found: ignore

  publish:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure
        run: ls -R artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: "🌸 Sakura Serial ${{ github.ref_name }}"
          body: |
            ## ✨ Changes

            - See [commits](${{ github.server_url }}/${{ github.repository }}/compare/${{ github.event.before }}...${{ github.sha }})

            ## 📦 Downloads

            | Platform | File |
            |----------|------|
            | 🪟 Windows | `sakura-serial-*-setup.exe` |
            | 🍎 macOS | `sakura-serial-*.dmg` |
            | 🐧 Linux | `sakura-serial-*.AppImage` / `.deb` |

            > Run the installer to upgrade. Previous settings are preserved.
          files: |
            artifacts/windows-latest/*.exe
            artifacts/macos-latest/*.dmg
            artifacts/ubuntu-latest/*.AppImage
            artifacts/ubuntu-latest/*.deb
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
